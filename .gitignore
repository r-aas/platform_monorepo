# Local overrides
envs/*.local
.env
.env.local

# Task artifacts
.task/

# Session state (may contain credentials)
RESUME.md

# Helm chart dependencies (downloaded by `helm dep update`)
charts/*/charts/*.tgz

# OS junk
.DS_Store
