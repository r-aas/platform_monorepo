langfuse:
  langfuse:
    ingress:
      enabled: true
      className: nginx
      hosts:
        - host: langfuse.platform.127.0.0.1.nip.io
          paths:
            - path: /
              pathType: Prefix
