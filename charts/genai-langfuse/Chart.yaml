apiVersion: v2
name: genai-langfuse
description: Langfuse LLM observability (bundles ClickHouse + Redis)
type: application
version: 0.1.0
appVersion: "3"

dependencies:
  - name: langfuse
    version: "1.5.22"
    repository: "https://langfuse.github.io/langfuse-k8s"
