# ArgoCD — k3d local overrides

global:
  domain: argocd.platform.127.0.0.1.nip.io

server:
  ingress:
    enabled: true
    ingressClassName: nginx
    annotations:
      nginx.ingress.kubernetes.io/backend-protocol: HTTP
    hostname: argocd.platform.127.0.0.1.nip.io
