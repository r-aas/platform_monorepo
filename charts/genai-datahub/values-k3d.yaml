datahub:
  datahub-frontend:
    ingress:
      enabled: true
      className: nginx
      hosts:
        - host: datahub.platform.127.0.0.1.nip.io
          paths:
            - /

  datahub-gms:
    ingress:
      enabled: true
      className: nginx
      hosts:
        - host: datahub-gms.platform.127.0.0.1.nip.io
          paths:
            - /
