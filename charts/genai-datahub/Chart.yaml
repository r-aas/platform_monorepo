apiVersion: v2
name: genai-datahub
description: DataHub metadata catalog — GMS, frontend, actions
type: application
version: 0.1.0
appVersion: "v1.4.0.3"

dependencies:
  - name: datahub
    version: "0.8.24"
    repository: "https://helm.datahubproject.io/"
