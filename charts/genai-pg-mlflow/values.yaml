postgresql:
  fullnameOverride: genai-pg-mlflow
  auth:
    username: mlflow
    password: mlflow
    database: mlflow
  primary:
    resources:
      requests:
        memory: 128Mi
        cpu: 100m
      limits:
        memory: 256Mi
        cpu: "0.5"
    persistence:
      enabled: true
      size: 1Gi
