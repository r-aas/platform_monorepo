n8n:
  ingress:
    enabled: true
    className: nginx
    hosts:
      - host: n8n.mewtwo.127.0.0.1.nip.io
        paths:
          - path: /
            pathType: Prefix
  main:
    persistence:
      enabled: true
      storageClass: local-path
