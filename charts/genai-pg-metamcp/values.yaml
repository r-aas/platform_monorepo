postgresql:
  fullnameOverride: genai-pg-metamcp
  auth:
    username: metamcp
    password: metamcp
    database: metamcp
  primary:
    resources:
      requests:
        memory: 128Mi
        cpu: 100m
      limits:
        memory: 256Mi
        cpu: "0.5"
    persistence:
      enabled: true
      size: 1Gi
