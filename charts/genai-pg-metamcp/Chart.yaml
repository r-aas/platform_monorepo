apiVersion: v2
name: genai-pg-metamcp
description: PostgreSQL for MetaMCP gateway
type: application
version: 0.1.0
appVersion: "16"

dependencies:
  - name: postgresql
    version: "18.5.6"
    repository: "https://charts.bitnami.com/bitnami"
