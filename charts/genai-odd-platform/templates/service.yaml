apiVersion: v1
kind: Service
metadata:
  name: {{ include "genai-odd-platform.fullname" . }}
  labels:
    {{- include "genai-odd-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "genai-odd-platform.selectorLabels" . | nindent 4 }}
